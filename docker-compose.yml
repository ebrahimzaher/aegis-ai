services:
  api:
    build: .
    container_name: aegis-ai-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data/chroma:/app/data/chroma
    restart: unless-stopped